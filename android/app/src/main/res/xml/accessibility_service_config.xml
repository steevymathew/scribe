<?xml version="1.0" encoding="utf-8"?>
<!--
  The bubble's permissions, kept as narrow as the feature allows.

  canRetrieveWindowContent is unavoidable: without it Scribe cannot tell whether the
  focused view is a text field, and cannot use ACTION_SET_TEXT to type into it. There is
  no narrower API that does either.

  What is avoidable is avoided — no key-event filtering, no gesture capture, no
  fingerprint access, and no notification access.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/a11y_description"
    android:summary="@string/a11y_summary"
    android:accessibilityEventTypes="typeViewFocused|typeViewClicked|typeWindowStateChanged|typeWindowContentChanged|typeViewTextSelectionChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:settingsActivity="dev.smantics.scribe.ui.MainActivity" />
